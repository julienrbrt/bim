Write a Foundry proof-of-concept test that demonstrates a third-party attacker stealing or permanently locking funds by exploiting the following vulnerability.

**Chain ID:** {{.ChainID}}
**Contract Address:** `{{.Address}}`
**Vulnerability:** {{.Title}}
**Severity:** {{.Severity}}
**Category:** {{.Category}}
**Affected Function:** `{{.AffectedFunction}}`
**Description:** {{.Description}}

**Relevant Source Code:**

```solidity
{{.RelevantSource}}
```

Requirements:
- Output ONLY the Solidity test file, no explanation before or after.
- The test must compile with Foundry (forge test).
- Use pragma solidity ^0.8.0 or higher.
- Import "forge-std/Test.sol".
- Name the contract ExploitTest and extend Test.
- Include a setUp() function that forks mainnet using vm.createSelectFork().
- Include a test_exploit() function that demonstrates the attack.
- The attacker MUST be an unprivileged address (not the owner, admin, or any privileged role). Use vm.prank() with a fresh attacker address.
- Do NOT use vm.prank() with the contract owner or any admin address — the exploit must be executable by anyone.
- If the attack requires a flash loan, implement the appropriate callback interface (e.g. IFlashLoanReceiver) and demonstrate the full borrow-exploit-repay flow.
- Use assertions to prove funds were stolen: the attacker's balance after the exploit must be greater than before (e.g. assertGt(attackerBalanceAfter, attackerBalanceBefore)).
- Add inline comments explaining each step of the attack sequence.
- Log the attacker's profit using console.log for clarity.
